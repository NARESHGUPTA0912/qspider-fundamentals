/* var (Awara)
    - Multiple time declaration
    - Multiple time initialization

  let (Pagal)
    - One time declaration
    - Multiple time initialization 

  const (Deewana)
    - One time declaration
    - One time initialization


var a = 10 ;  
var a = 100;
var a = 1000;
    a = 20;


let b = 10;
    b = 200;
    b = 2000;
    b = 30;
console.log(b);


const c = 40;
console.log(c);
*/

/**
 * ! Identifier:- 
 * Identifiers are user given names that is used in writing a program
 * 
 * - name of a class
 * - name of a method
 * - name of a variable
 * - name of a package
 * - name of any other class member
 */

const age = 10;
const userName = "Tinku";

/**
 * ! Operators:- 
 * Operators are predefined symbols which is used to perform some specific task.
 * 
 * ! Operand:-
 * The values needed by an operator is known as Operands.
 

// JS = Dynamically Typed Language
const a = "10";
const b = "20a";

console.log(a > b);  // "10" > "20a"   --->   10 > 20
console.log(a < b);  // "10" < "20a"   --->   10 < 20


* ! Logical AND (&&)
console.log( 0 && 3);        // 0
console.log( 0.5 && 0.7);    //0.7
console.log(3 && 4);         // 4
console.log(" " && "");      // ""

* ! Logical OR (||)
console.log( 4 || 3);   // 4
console.log(  3 || 0);  // 3
console.log(0 || 0.5);  // 0.5
console.log( 0 || null);  // null

const isLogin = false;

if(!isLogin)
{
  console.log("Please Login...");
}
*/

/**
 * ! Ternary Operator :- Conditional Operator

const isLogin = true;
const res =  isLogin ? "Welcome User" : "Please Login";
console.log(res);
 */

